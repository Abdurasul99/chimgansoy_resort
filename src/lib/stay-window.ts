/**
 * С какой даты принимаем заезды в глэмпинг и шале.
 *
 * Отдельным модулем, а не константой в серверном действии: файл с "use server"
 * умеет экспортировать только асинхронные функции, и форме эта дата тоже нужна —
 * чтобы календарь не предлагал того, что сервер всё равно отклонит.
 *
 * Отказ после заполнения формы гость читает как поломку сайта, а не как правило,
 * поэтому проверок две: календарь не даёт выбрать, сервер не даёт обойти.
 */
export const STAY_OPENS_AT = "2026-08-15";
